start = t_text
t_text = element text {body+}
#
body = element body {division+}
division = element div {d_type ,opener+, p+, closer+}
d_type = attribute type {text}
opener = element opener {dateline+, opensalute}
dateline = element dateline {date, placename}
opensalute = element salute {text}
date = element date {when, text}
when = attribute when {text}
placename = element name {n_type, text}
n_type = attribute type {text}
#
emote = element emote {e_type, text}
e_type = element e_type {positive | negative | neutral}
positive = attribute positive {text}
negative = attribute negative {text}
neutral = attribute neutral {text}
#
factor = element factor {f_type, text}
f_type = element f_type {sense | trauma | battle | prep | conditions}
sense  = attribute sense {text}
trauma = attribute trauma {text}
battle = attribute battle {text}
prep = attribute prep {text}
conditions = attribute conditions {text}
#
p = element p { mixed { ( emote | factor )* } }
#
closer = element closer {closesalute, signed}
closesalute = element salute {text}
signed = element signed {signedname}
signedname = element name {p_type, text}
p_type = attribute type {text}
#
#TEI types have lots of values:
#Three types: d_type is "division type", n_type is "name of place type", and p_type is "person type".
#Two names: placename and signedname
#Two salutes: opensalute and closesalute#
#What element will we use for the personal info at the beginning of the letters?
